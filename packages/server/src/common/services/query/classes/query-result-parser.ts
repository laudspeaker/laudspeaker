import {
  Query,
} from "../";

export class QueryResultParser {
  parse(rawResult: any) {
    return this.parseResult(rawResult);
  }

  private parseResult(rawResult: any) {
    return rawResult;
  }

  
}
