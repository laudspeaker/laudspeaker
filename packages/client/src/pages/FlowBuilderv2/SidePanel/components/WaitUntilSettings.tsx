import React, { FC } from "react";
import { SidePanelComponentProps } from "../FlowBuilderSidePanel";

const WaitUntilSettings: FC<SidePanelComponentProps> = ({
  nodeData,
  setNodeData,
}) => {
  return <div>WaitUntilSettings</div>;
};

export default WaitUntilSettings;
